version: '3.8'

services:
  app:
    build: .
    volumes:
      - .:/app
      - /app/venv # Avoid overriding venv if it exists
    env_file:
      - .env
    restart: always
    container_name: autoffiliate_app
